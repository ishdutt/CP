#include <bits/stdc++.h>
using namespace std;
typedef long long      ll;

int solve(vector<int>&a,int it,vector<int>&denomination,int din,set<int>taken,int sum)
{
    if(sum==0)  return 0;

    x
}


int main() 
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    
    int n;
    cin>>n;
    vector<int>a(n,0);
    for(int i=0;i<n;i++)
        cin>>a[i];
    
    vector<int>denomination;
    for(int i=0;i<239;i++)
        denomination.push_back(i);

    set<int>taken;

    solve(a,0,denomination,0,taken,239);

    return 0;        
}
    
